# For local frontend dev against the standalone backend on port 8000 (default):
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Same-origin when served by `inferencache serve`:
# NEXT_PUBLIC_API_URL=/api
