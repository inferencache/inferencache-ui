# Same-origin when served by `promptcache serve` (default).
NEXT_PUBLIC_API_URL=/api

# For local frontend dev against the standalone backend on port 8000:
# NEXT_PUBLIC_API_URL=http://localhost:8000/api
